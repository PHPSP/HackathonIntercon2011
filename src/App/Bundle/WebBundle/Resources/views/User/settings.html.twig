{% extends 'AppWebBundle::base.html.twig' %}

{% block current_page%}Configurações{% endblock %}

{% block content %}
<div class="row">
	<div class="span16">
		<h1>Sua conta</h1>

		<form action="{{ path('user_settings_save') }}" method="post" {{ form_enctype(form) }}>
		    {{ form_widget(form) }}
			<div class="actions">
				<button type="submit" class="btn primary">Salvar</button>
			</div>
		</form>

		<ul class="record_actions">
		    <li>
		        <a href="{{ path('user') }}">
		            Back to the list
		        </a>
		    </li>
		</ul>
	</div>
</div>

{% endblock %}

